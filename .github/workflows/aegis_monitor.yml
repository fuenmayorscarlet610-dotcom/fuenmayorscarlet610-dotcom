name: Monitor de Vigilancia AEGIS
on: [push]

jobs:
  telegram_notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar Alerta a Scarlet
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          message: |
            🛡️ **PROTOCOLO AEGIS: ACTIVADO**
            
            Hola Scarlet, se ha detectado una actualización en tu infraestructura de GitHub.
            
            📍 **Evento:** Actualización de Código
            ✅ **Estado:** Sistema Verificado y Seguro.
            
            "La mejor reparación es la que no tiene que repetirse."
