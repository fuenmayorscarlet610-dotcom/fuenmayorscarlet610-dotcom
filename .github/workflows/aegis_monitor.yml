name: Monitor AEGIS
on: [push]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar Alerta
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🛡️ PROTOCOLO AEGIS: ACTIVADO
            Hola Scarlet, sistema actualizado con éxito en Caracas.
